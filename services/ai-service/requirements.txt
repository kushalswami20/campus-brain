# Core runtime.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
structlog==24.4.0
httpx==0.28.1
python-multipart==0.0.19

# Ingestion — document parsing & OCR (used on the real path; the local text +
# fake-provider path needs none of these).
pymupdf==1.25.1
python-docx==1.1.2
pillow==11.0.0
pytesseract==0.3.13

# Real providers, activated automatically when their keys are configured.
openai==1.57.4
pinecone-client==5.0.1

# Multi-agent RAG stack. LangGraph orchestrates the agent graph; rank-bm25 is the
# sparse half of hybrid retrieval. Cross-encoder reranking uses sentence-
# transformers when installed (heavy, torch-backed) and falls back to a
# deterministic reranker otherwise — install it in the GPU/prod image:
#   sentence-transformers==3.3.1
langgraph==0.2.53
rank-bm25==0.2.2
